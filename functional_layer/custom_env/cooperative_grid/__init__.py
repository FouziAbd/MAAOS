"""CooperativeGrid — a small deterministic two-agent grid world (PettingZoo ParallelEnv) with a
MiniGrid-style renderer. The authoritative backend of the MAAOS domain `cooperative_grid`;
it knows nothing about MAAOS (see `domains/cooperative_grid/environment.py` for the binding).
"""
