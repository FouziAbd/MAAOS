"""P0-P4 test suite.

Default tests are deterministic and offline: no live LM, no network, no environment stepping.
Run with either:

    python -m unittest discover -s tests -t .
    pytest tests            # if pytest is installed
"""
